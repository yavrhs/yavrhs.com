// -----------------------------------------------------------------------
// SITE-WIDE SETTINGS
// -----------------------------------------------------------------------
// To hide the EXHIBITIONS link from the nav on every page (e.g. before
// your first exhibition is public), set this to false. The exhibitions
// pages themselves keep working if someone has the direct link — this
// only removes it from the menu.
// -----------------------------------------------------------------------
const SITE_CONFIG = {
  showExhibitions: true,
};
